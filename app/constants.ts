// Breakpoints (en pixels)
export const BREAKPOINTS = {
    MOBILE: 480,
    TABLET: 769,
    DESKTOP: 1025,
} as const;

// Durées d'animation (en ms)
export const ANIMATION = {
    DURATION: 500,
    TAP_FEEDBACK: 500,
} as const;
